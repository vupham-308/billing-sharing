name: Deploy Spring Boot Backend to Oracle VPS

on:
  push:
    branches: [ "main", "master" ]
    paths:
      - "be/**"
      - ".github/workflows/deploy-backend.yml"
  workflow_dispatch:

jobs:
  build-and-deploy:
    name: Build & Deploy Spring Boot (Port 6969)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: maven
          cache-dependency-path: 'be/pom.xml'

      - name: Build JAR with Maven
        working-directory: be
        run: mvn clean package -DskipTests -B --no-transfer-progress

      - name: Copy JAR to Oracle VPS via SSH Key
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_USER || 'ubuntu' }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          port: ${{ secrets.ORACLE_PORT || 22 }}
          source: "be/target/*.jar"
          target: "/home/ubuntu/app/billing-sharing/"
          strip_components: 2
          overwrite: true

      - name: Restart Backend Docker Container via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ORACLE_HOST }}
          username: ${{ secrets.ORACLE_USER || 'ubuntu' }}
          key: ${{ secrets.ORACLE_SSH_KEY }}
          port: ${{ secrets.ORACLE_PORT || 22 }}
          script: |
            echo "1. Chuẩn bị thư mục và chuẩn hóa tên file JAR..."
            mkdir -p /home/ubuntu/app/billing-sharing
            cd /home/ubuntu/app/billing-sharing

            LATEST_JAR=$(ls -t *.jar 2>/dev/null | grep -v '^billing-sharing.jar$' | head -1)
            if [ -n "$LATEST_JAR" ]; then
              mv -f "$LATEST_JAR" billing-sharing.jar
            fi
            chmod +x billing-sharing.jar

            echo "2. Khởi động lại container billing-sharing-backend (Port 6969)..."
            # Dừng và xóa container cũ để luôn áp dụng cấu hình mới nhất
            docker stop billing-sharing-backend 2>/dev/null || true
            docker rm billing-sharing-backend 2>/dev/null || true

            APP_PORT=6969
            ENV_FILE_FLAG=""
            if [ -f /home/ubuntu/app/billing-sharing/.env ]; then
              ENV_FILE_FLAG="--env-file /home/ubuntu/app/billing-sharing/.env"
              ENV_PORT=$(grep -E '^[[:space:]]*SERVER_PORT=' /home/ubuntu/app/billing-sharing/.env | cut -d '=' -f2 | tr -d '\r"' | xargs || true)
              if [ -n "$ENV_PORT" ]; then
                APP_PORT="$ENV_PORT"
              fi
            fi

            echo "-> Port Backend sử dụng: $APP_PORT"

            # Khởi chạy container tối ưu tài nguyên cho VPS:
            # - Giới hạn RAM 768m, CPU 0.5 core
            # - --restart on-failure:1 chỉ thử lại đúng 1 lần duy nhất nếu lỗi
            # - -XX:+UseSerialGC và -Xms128m -Xmx384m tiết kiệm CPU & RAM tối đa
            docker run -d \
              --name billing-sharing-backend \
              --restart on-failure:1 \
              --memory="768m" \
              --cpus="0.5" \
              --net=host \
              $ENV_FILE_FLAG \
              -v /home/ubuntu/app/billing-sharing:/app \
              eclipse-temurin:17-jre \
              java -Xms128m -Xmx384m -XX:+UseSerialGC -jar /app/billing-sharing.jar --server.port="$APP_PORT"

            echo "3. Kiểm tra sức khỏe Backend (Port $APP_PORT)..."
            READY=0
            for i in {1..10}; do
              sleep 3
              STATUS=$(docker inspect -f '{{.State.Status}}' billing-sharing-backend 2>/dev/null || echo "unknown")
              if [ "$STATUS" != "running" ]; then
                echo "LỖI: Container bị dừng hoặc crash (Status: $STATUS). Log chi tiết:"
                docker logs --tail 40 billing-sharing-backend
                exit 1
              fi

              HTTP_CODE=$(curl -s -o /dev/null -w "%{http_code}" "http://localhost:${APP_PORT}/api/v1/auth/me" || echo "000")
              if [ "$HTTP_CODE" != "000" ]; then
                echo "-> Backend phản hồi HTTP Status: $HTTP_CODE (Sẵn sàng sau $((i*3))s)"
                READY=1
                break
              fi
              echo "   Đang chờ Spring Boot sẵn sàng ($((i*3))s)..."
            done

            docker ps --filter "name=billing-sharing-backend"

            if [ $READY -eq 1 ]; then
              echo "Triển khai Backend lên VPS Oracle ở cổng $APP_PORT hoàn tất!"
            else
              echo "Cảnh báo: Backend cần thêm thời gian để sẵn sàng. Kiểm tra log bằng: docker logs -f billing-sharing-backend"
            fi
